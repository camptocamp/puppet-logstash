# defaults for logstash

# Start logstash on boot?
START=yes

# pulled in from the init script; makes things easier.
NAME=logstash-<%= @name %>

# location of java
JAVA=/usr/bin/java

# arguments to pass to java
LS_JAVA_OPTS="<%= @java_opts %> -Djava.io.tmpdir=/var/lib/logstash/"

PIDFILE=/var/run/logstash.pid

# user id to be invoked as
LS_USER=<%= @process_owner %>

# location of the logstas jar file
#LS_JAR=/opt/logstash/logstash.jar

# logstash home location
#LS_HOME=/opt/logstash

# logstash log directory
LOG_DIR=/var/log/logstash

# logstash log file
LOG_FILE=$LOG_DIR/$NAME.log

# logstash configuration directory
CONF_DIR=/etc/logstash/<%= @name %>.conf.d

# Open file limit
OPEN_FILES=16384

# Nice level
NICE=19

# Set LogStash options
#LS_OPTS="--log ${LOG_FILE}"

# number of filter processing threads
FILTER_THREADS=<%= @filter_threads %>
